#include <stdio.h>
#include <stdlib.h>

/**
 * main - Prints the alphabetic
 *
 * Return: Always (Success)
 */
int main(void)
{
	char d;

	for(d = 'a';d <= 'z'; d++)
	    putchar(d);

	putchar('\n');
	return (0);
}
