#include "main.h"

/**
 * mul - Multiplies two integers
 * @x: The first number to multiply
 * @y: The second number to multiply with first
 *
 * Return: The result of multiplication between x param anf y param
 */
int mul(int x, int y)
{
	return (x * y);
}
