#include "holberton.h"
#include <stdio.h>

/**
 * main - check the code
 *
 * Return: Always 0.
 */
int _abs(int)
{
	if (c < 0)
	{
		int abs_val;
		abs_val = c * -1;
		return (abs_val)
	}
	return (c);
}
